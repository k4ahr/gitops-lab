apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: nginx
        env:
        - name: ENV
          value: "development"
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
